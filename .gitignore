# Will be ignored by default.
target/

# Don't track dependencies in a .cargo/ directory
.cargo

# Don't track the crate lock file
Cargo.lock

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which are not needed
*.pdb

# Environment variables
.env
